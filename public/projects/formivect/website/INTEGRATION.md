# Formivect website integration

Publish only this public folder. Copy its complete contents to public/projects/formivect/ in harbegold/harrisonbgoldberg.com. Preserve Acuivect, Bitzy V1 and every other existing project.

Copy website/formivect.ts to data/formivect.ts, import FORMIVECT_PROJECT into constants.tsx and add it as a new entry with ID formivect. Do not replace or rename any existing project. Link to /projects/formivect/. Add the Formivect knowledge entry from chat-summary.md to api/chat.ts; preserve existing passages.

Route the trailing-slash URL to index.html, redirect the path without the slash and exclude this subtree from the SPA fallback. The public folder is a static artifact, excluded from TypeScript compilation; the copied data adapter is compiled.

Verify PUBLIC-MANIFEST.json, ZIP/source/copy/build/Git/HTTP bytes, all links/images/PDFs, feature search/filter/status labels, desktop/mobile layout, stable original project data and TypeScript/build. The public-only ZIP contains only this public folder's contents. Outer logs, original software/source/installers, private histories, credentials and full transfer/pitch archives remain private.

Content reviewed September 10, 2026. Current and potential capabilities must stay distinct; no trained Formivect model or independent CAD-suite parity is claimed.
